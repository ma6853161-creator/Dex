<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Only standard, publicly-documented permissions. No signature|system
         permissions are requested here — see README "Legal boundary" section
         for why we deliberately avoid Samsung's signature-level approach. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-feature android:name="android.hardware.wifi" android:required="true" />

    <application
        android:allowBackup="false"
        android:icon="@android:drawable/ic_menu_view"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Material.Light">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:screenOrientation="landscape"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- "Screen In" feature: foreground service that owns the TCP transport,
             the MediaCodec decoder and the render Surface. Equivalent role to
             SmartMirroring's ScreenSharingReady/SecondScreenPlayer pair, but
             reimplemented from scratch with our own protocol. -->
        <service
            android:name=".receiver.ReceiverService"
            android:exported="false"
            android:foregroundServiceType="mediaProjection" />

        <!-- Quick Settings tile toggle for receiver mode, equivalent in *role*
             to SmartMirroring's ScreenSharingTile. -->
        <service
            android:name=".ui.ScreenInTileService"
            android:exported="true"
            android:icon="@android:drawable/ic_menu_view"
            android:label="@string/tile_label"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

    </application>
</manifest>
